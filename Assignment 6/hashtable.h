#ifndef HASHTABLE_H
#define HASHTABLE_H

#include <string>
#include <fstream>

class Hashtable {
    private:

    public:
        Hashtable() { }

        ~Hashtable() {}

        bool isKey(std::string check) { return false; }
};

#endif